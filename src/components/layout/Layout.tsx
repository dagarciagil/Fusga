import { Navbar } from "./Navbar";

// El componente Layout se encarga de separar las responsabilidades del componente APP, se usa para cargar los componentes generales, como footer, navbar, etc..
export const Layout =() => {
    return (
      <Navbar />
    );
}